import { CalexponentPipe } from './calexponent.pipe';

describe('CalexponentPipe', () => {
  it('create an instance', () => {
    const pipe = new CalexponentPipe();
    expect(pipe).toBeTruthy();
  });
});
